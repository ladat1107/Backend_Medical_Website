import db from "../models/index";

const insertMedicine = async () => {
    try {

    } catch (error) {
        console.log(error);
        console.log("Lỗi hệ thống");
    }
}
module.exports = {
    insertMedicine
}